# 大模型时代复利的第一步，创建 CLAUDE.md

> **创建 Skill 之前，请先尝试创建 CLAUDE.md 文件。**

我前段时间在写一篇 IEEE 期刊的英文论文，分布式优化方向。Introduction 改到第三版，我已经被自己折磨疯了。

每次开新会话，我都得跟 Claude 说一遍同样的话。

英文摘要要 4-5 句、第一句问题陈述、第二句方法概述、第三句关键结果、第四句意义升华；变量符号要斜体但向量加粗；公式编号用 `(1)` 不用 `Eq.(1)`；引用格式按 IEEE 标准；术语前后要统一，比如 `state-of-charge` 全文不能一会写成 `SOC` 一会写成 `SoC`；图表标题英文首字母大写但介词不大写……

写到第三遍的时候我就崩溃了。

我已经把这堆要求说了 N 遍了。

每次开新会话，就得重新说一遍。每次换个论文项目，又得重新说一遍。我自己脑子里那套写作规范，明明已经成型很久了，怎么搞得像每次都在从零教一个新员工。

那个时候我才意识到，我一直在做一件特别傻的事情。

其实Claude里有一个工具，可以让 Claude 一打开项目就自动知道我所有的规则、所有的偏好、所有踩过的坑。

它叫 **CLAUDE.md**。

---

## 一、CLAUDE.md 跟 Skill 到底什么关系

聊到 CLAUDE.md，可能很多用 Claude Code 的读者会想到另一个东西，叫 **Skill**。

Skill 是 Claude 官方推出来的一种「能力封装」机制。你写一个完整的 Markdown 文件，配上 yaml frontmatter，描述触发条件、调用流程、注意事项，Claude 在合适的时候会自动把它加载进来。

Skill 强是真的强。但门槛比CLAUDE.md高，**建议创建skill之前先尝试创建 CLAUDE.md**。

Skill像是给 Claude 装一个完整的「插件」，写一次能复用很多次。但你得有一定的编程经验和耐心去打磨它，修改skill相对麻烦，并且会担心把skill修改坏。

CLAUDE.md 就完全不一样。

它就是个普通的 markdown 文件，记事本能打开，肉眼能看明白，鼠标点两下就能改。

打个比方。

如果说 **Skill 是把一个完整的同事招进公司**，给他配工位、给他写岗位说明书、给他做岗前培训，那 **CLAUDE.md 就是你贴在自己办公桌上的一张便利贴**，专门告诉每一个来你工位上帮忙的人，「我这个项目，有几个习惯」。

便利贴不需要培训，不需要测试，不需要 yaml。你想加一条就加一条，想删一条就删一条。Claude 每次进来你这个项目，第一件事就是把这张便利贴扫一眼，然后才开始干活。

你想想这两个东西的区别。

**Skill是「工具」，CLAUDE.md是「常识」。**

Skill 解决的是「我有一个复杂的工作流，希望 Claude 能复用」的问题。CLAUDE.md 解决的是「我这个项目有一堆约定俗成的规则，希望 Claude 一来就知道」的问题。

绝大多数日常工作，你需要的是后者。

---

## 二、为什么我说每个项目都该有一份 CLAUDE.md

聊到这里，可能有读者会问，那 CLAUDE.md 凭什么这么重要？

我自己的感受，主要有四点。

**第一，它简单到不像话。**

整个文件就是一段段大白话，你想写什么写什么。比如我写论文的 CLAUDE.md 里，第一条规则是「英文摘要 4-5 句」。第二条是「变量斜体，向量加粗」。第三条是「术语前后必须一致」。

你能看懂，我能看懂，Claude 也能看懂。

没有 yaml，没有 frontmatter，没有触发条件，没有 SOP 流程图。一段大白话，告诉它怎么干。

**第二，它可视化。**

memory 是 Claude 自己管理的一个黑盒，你不知道它具体记了什么，也很难直接修改。MCP 是远程服务，调试都得看日志。Hooks 是配在 settings.json 里的脚本，普通用户根本不会去碰。

CLAUDE.md 不一样。它就摆在那里，跟你的代码文件平起平坐，git 能追踪，VS Code 能打开，小白用户也能看懂。

你今天加了什么规则，明天回来一看就知道。

**第三，它可以随时改。**

这点特别重要。我做项目的时候，经常是边做边发现新的坑。比如某个变量符号容易写错，某个公式格式 Claude 老搞错，某个文件路径它老记不住。

发现一个，我就让它写进 CLAUDE.md。

下一次再开新会话的时候，Claude 自动知道这些坑，不用我再说第二遍。

**第四，它跟 git 走，团队能共享。**

如果你这个项目是几个人一起做的，CLAUDE.md 一旦 commit 上去，所有人的 Claude 都会用同一套规则。一致性的问题，就这样解决了。

我自己的体感是，写了 CLAUDE.md 之后，整个项目的协作效率会上一个台阶。

你不再需要每次开新会话都「教一遍」，Claude 不再每次都「忘一遍」。这才是大模型工具应该有的样子，**不应该是每次都从零开始**。

---

## 三、怎么创建？真的就是一句话

好，重要性聊完了。可能有人会问，那这个 CLAUDE.md 到底怎么创建？

其实就是一句话。

> **请帮我创建这个项目的 claude.md 文件。请在这个项目的CLAUDE.md名字前加入项目名前缀，使其不会跟别的 CLAUDE.md弄混。**

就这么简单。

Claude Code 自带一个 `/init` 命令，专门用来扫描你当前的项目目录，分析代码结构、识别构建系统、检测测试框架，然后帮你生成一个起始版本的 CLAUDE.md。

如果你嫌 /init 太机械化，直接跟它说「请帮我创建这个项目的 claude.md 文件」也行，效果差不多，甚至有时候更贴你的项目语境。

文件地址，就放在这个项目的同一个目录下。

如果你这个目录里已经有别的 CLAUDE.md 了，比如你之前别的子项目留下的，建议起一个更具体的名字。

比如帮助论文编写的叫「论文编写CLAUDE.md」。
帮助回复审稿意见的叫「审稿意见回复Claude.md」。。

**加个项目名前缀，就不会跟别的 CLAUDE.md 弄混。**

一个迷你版的 CLAUDE.md 大概长这样，让你有个直观印象。

```markdown
# 摘要 (Abstract)
- 5-8 句、150-250 词
- 第一句问题背景，第二句 "This paper proposes..."
- 用主动语态，每句一个核心信息
- 描述本文工作用过去时（proposed / developed）

# 引言 (Introduction)
- 第一段，大背景 → 应用 → 引出方法需求
- Contributions 固定三个，句子短，不加引用
- 文献综述分组串联，每组末尾指出共同 gap

# 公式与术语
- IMPORTANT，符号定义后全文一致，转置统一用 $^\top$
- 缩写只在正文首次出现时展开（摘要展开不算）
- 全文术语一致，如选 UAV 则不混用 robot

# 用词规范
- 用 "distributed" 而非 "decentralized"
- Section 开头用被动语态，"We" 留给 Contributions

# 数据诚信
- IMPORTANT，所有数据必须来自程序实际输出，绝对不可凭空捏造
```

就这么五段，二十多行。

但你下次打开 Claude Code 写新论文，它就知道摘要要几句、引言怎么开头、术语怎么统一、Section 开头用什么语态、哪些用词得换掉、哪些数据是绝对禁区。

---

## 四、怎么往里加东西？还是一句话

创建好了，怎么往里加东西？

也是一句话。

> **把这条更新到该项目文件夹的 CLAUDE.md 里，确保下次别再犯同样的错误。**

我自己用得最多的就是这句。

举个例子。我前两天用 Claude Code 跑一个 MILP 的实验，结果它把超时时间设成了 10 分钟，跑到一半就被 kill 了。

我跟它说，MILP 实验通常需要 1 小时以上的超时时间，把这条更新到该项目文件夹的 CLAUDE.md 里，确保下次别再犯同样的错误。

它就乖乖把这条规则加进去了。

第二天我开新会话跑别的实验，提到 MILP，它就自动用 1 小时的超时时间。

整个过程行云流水。

你不需要去操心怎么写，不需要纠结放在文件的哪个位置，不需要担心格式对不对。Claude 自己会给你整理好。

**你的工作就是发现一个坑，告诉它「记一下」，剩下的它来。**

---

## 五、什么时候特别需要？拿我写论文这件事举例

那么问题来了，什么时候需要给一个项目创建 CLAUDE.md？

我的回答可能有点偏激，但我自己确实是这么做的。

**每个项目都需要。**

无论项目大小、无论项目类型、无论项目是临时玩玩的还是长期跟踪的，只要你打开 Claude Code 在这个目录下干活，就给它一个 CLAUDE.md。

哪怕一开始只有一句话也行。

「这个项目是用来做 X 的，主要文件是 Y，注意 Z」。

就这一句话，就能让 Claude 不至于一脸懵地进来。

但有些场景，特别需要 CLAUDE.md。

那种**每次都要重复说一样的话的工作**。比如写论文，比如写报告，比如调一类套路化的实验，比如生成符合特定规范的图表。

我自己最早把 CLAUDE.md 这玩意用爽的场景，就是写论文。

写论文这件事吧，看起来每篇都是新课题，但其实底层规范非常固定。

英文摘要怎么开头，引言要不要先讲背景再点 gap，Related Work 用哪种组织方式，方法部分公式编号怎么管，实验图怎么排，参考文献用什么格式，变量符号要不要斜体，术语前后要不要一致，缩写第一次出现要不要全称……

每一项都有套路。每一项 Claude 都可能写错。

我把这套东西全部沉淀到了一份 CLAUDE.md 里，大概包含这些内容：

摘要标准结构，4-5 句话，第一句问题陈述、第二句方法概述、第三句关键结果、第四句意义升华。

引言三段式，背景与动机、文献 gap、本文贡献。

公式排版规范，编号格式、变量斜体、向量加粗、矩阵粗斜体。

术语统一表，state-of-charge 全文必须一致拼写、distributed optimization 不要混用 decentralized。

参考文献格式，IEEE 标准、作者首字母缩写、会议名简写规则。

我自己常踩的英文错误清单，比如 a/an 误用、单复数不一致、定冠词漏掉、动词时态混乱、句子主语缺失。

我现在每开一个新论文项目，第一件事就是把这份 CLAUDE.md 复制到当前项目目录下。

Claude 看一眼，写出来的英文摘要、引言、Related Work，直接就是我惯用的句式和措辞，不需要每次都教一遍。

**关键是这一点。**

写论文这件事，我自己一定会反复改、反复审、最后一字一句过一遍才提交。Claude 帮我做的是初稿和润色，**最终把关的还是人**。

这种流程下，CLAUDE.md 的价值是最大化的。它能让初稿的质量上一个台阶，让我从「重复教 Claude 规则」中解放出来，把精力集中在「判断这个论点站不站得住」「这个实验设计合不合理」这些真正需要人来做的事情上。

如果你不写论文，那就把上面这段话里的「论文」替换成你正在做的那件高频重复的事。

可能是你每周都要写的那份周报。
可能是你那个固定格式的客户分析。
可能是你那一类套路化的代码迁移。
可能是你公司那套独有的注释规范、变量命名、目录结构。

不管是什么，**只要你发现自己每次开新会话都要重复说同一堆话，那就是 CLAUDE.md 该出场的地方**。

它不仅是规则的集合，更是你这个领域的「打法」沉淀。

---

## 六、官方维护建议（六条精华）

聊完了重要性、创建、更新和使用场景，最后聊聊维护。

这块我特意去翻了一下 Anthropic 官方的 best practices 文档、官方关于 Memory 的文档，还有 HumanLayer 写的 Writing a good CLAUDE.md。把里面的精华提炼一下。

**第一条，目标是 200 行以内。**

Anthropic 官方原话，「keep it short and human-readable」。HumanLayer 那边更激进，说他们自己的 CLAUDE.md 只有 60 行，less is more 原则永远适用。

为什么要简洁？

因为 CLAUDE.md 是每次会话都会被自动加载进 Claude 上下文窗口的。如果你写得特别长，重要的规则就会被淹没在噪音里，Claude 反而会忽略它们。

官方有个经验数据，前沿大模型可靠遵循约 150-200 条指令，而 Claude Code 的系统提示本身已经占了大概 50 条的空间。所以你的 CLAUDE.md 真正能塞进去的「有效指令」，差不多就 150 条。

**超过这个量，开始有规则被淹没。**

如果你发现 Claude 老是不听某条规则，第一反应不应该是「再加一条更严厉的」，而应该是「我的 CLAUDE.md 是不是太长了，那条规则是不是被淹没了」。

**第二条，对每一行问，删了它会不会让 Claude 犯错。**

这是官方推荐的剪枝标准。如果删掉某一行，Claude 还能正确干活，那这一行就该删。

我自己的体感，CLAUDE.md 最适合记的是这几类东西。

那些 Claude 没法从代码本身推断出来的命令。比如你这个项目的测试是用 npm run test:custom 跑的，不是默认的 npm test。

那些跟默认约定不一样的代码风格。比如你坚持用 ES Modules 不用 CommonJS。

那些非显而易见的坑。比如你的环境变量必须先 source 某个脚本才能生效。

那些项目特定的架构决策。比如这个仓库的 utils 目录其实是按业务分的，不是按工具类型分的。

那些容易踩的雷。比如某个 API 在并发场景下有竞态条件。

最不该写的是什么？

那些 Claude 自己读代码就能搞清楚的事情。
那些通用的编程常识，比如「写干净的代码」。
那些经常变的信息，比如版本号、发版日期。
那些大段的教程或解释。
那些 API 密钥、数据库连接串、密码（这些是绝对禁区，commit 上去会泄露）。

**写 CLAUDE.md 不是写文档，是给 Claude 写「便利贴」。**

**第三条，遇到 Claude 怎么都不听的规则，加权重词。**

官方原话，「You can tune instructions by adding emphasis」。

具体做法就是在那条规则前面加上 IMPORTANT、YOU MUST、CRITICAL 这种词。

加权重词是告诉模型，这条规则不是普通建议，是硬约束。

但这一招不能滥用，全文都 IMPORTANT 就等于一个都不 IMPORTANT。

我自己的习惯是，整个 CLAUDE.md 不超过 3-5 处 IMPORTANT 标记，留给真正关键的规则。

**第四条，CLAUDE.md 可以分层。**

Anthropic 官方提到，CLAUDE.md 文件可以放在多个位置，加载的时候是从当前目录向上遍历，所有发现的文件**拼接**到一起注入上下文，不会互相覆盖。

放在 ~/.claude/CLAUDE.md，对所有项目都生效。
放在项目根目录的 CLAUDE.md，对这个项目所有人生效，建议 commit 进 git。
放在 CLAUDE.local.md，对你个人生效但不影响团队，建议加进 .gitignore。
放在子目录的 CLAUDE.md，只在 Claude 访问该子目录文件时按需加载。

我自己的用法是，全局的偏好放在 ~/.claude/CLAUDE.md，比如「优先用 PowerShell 不用 cmd」、「下载默认装到 D 盘」、「永远不删 Normal.dotm」。

项目级的规则放在项目根目录，比如「这个项目用 EvoHarness 的 MAP-Elites 框架」。

子项目的细节放在子目录，比如 Paper 汇总目录下的 CLAUDE.md 专门写论文规范。

**层层叠加，互不干扰。**

**第五条，你还可以用 @path 引用其他文件。**

Anthropic 官方支持在 CLAUDE.md 里用 `@path/to/file` 的语法引用其他文件，被引用的文件会自动展开加载，最大递归深度 5 层。

这个特性巨好用。

比如你的项目有一份完整的 README，你不需要把内容复制到 CLAUDE.md 里，直接写 `@README.md` 就行。

或者你有一份独立的 git 工作流文档，写成 `@docs/git-instructions.md`，需要的时候自动加载。

这种「指针」式的写法，能保证你的 CLAUDE.md 始终精简，但又不丢失关键信息。

HumanLayer 那篇博客特别强调，「Prefer pointers to copies」，**宁可放指针也不要直接复制内容**。原因很简单，复制过来的内容会跟原文件不同步，几个月之后两边都对不上。

**第六条，把 CLAUDE.md 当代码维护。**

Anthropic 官方有一句话我特别认同，「**Treat CLAUDE.md like code**」。

定期 review 它。
有问题的时候 prune 它。
改完之后观察 Claude 的行为有没有真的变化。
跟着 git 走，让团队都能贡献。

调试的时候有个小技巧，在 Claude Code 里输入 `/memory` 命令，就能看到当前会话所有被加载的 CLAUDE.md 文件。如果你写的某条规则不生效，先 `/memory` 看看是不是根本没被加载。

最重要的是，它的价值是会随时间复利的。

你今天写一行规则，明天 Claude 就少犯一个错。
明天再写一行，后天又少犯一个错。
一年之后回头看，整个项目的 CLAUDE.md 已经沉淀了一套属于你和团队的「打法」。

这套打法，下一次招新人，下一次开新项目，下一次换平台，都能直接复用。

**这就是复利。**

---

## 七、大模型时代复利的第一步

我一直觉得，大模型时代真正改变工作方式的，不是模型本身有多聪明，而是你怎么跟模型协作。

模型在快速迭代，今天用 Opus 4.7，明天就是 5.0。但你跟模型之间那套「协作的常识」，是不会因为模型升级而失效的。

CLAUDE.md 就是承载这套常识的地方。

它简单到一句话就能创建。
它直观到任何人都能看懂。
它灵活到随时可以加可以删。
它可靠到每次会话都会被自动加载。

而它最大的好处，是让你跟 Claude 的协作**从「每次重新教」变成「持续积累」**。

**这就是大模型时代复利的第一步。**

如果你今天还没给自己的项目写 CLAUDE.md，我建议从今天就开始。

打开你最近在做的那个项目，跟 Claude 说一句「请帮我创建这个项目的 claude.md 文件」，三十秒钟之后你就有了第一份。

然后接下来一个月，每次发现 Claude 犯错，就跟它说一句「把这条更新到该项目文件夹的 CLAUDE.md 里，确保下次别再犯同样的错误」。

一个月之后回头看，你会发现自己的 Claude 已经不是当初那个每次都要重新教的 Claude 了。

它知道你的所有偏好。
它记得你踩过的所有坑。
它直接用你想要的方式干活。

**因为这些话，CLAUDE.md 已经替你说过了。**

---

> 仅以此文纪念加州大学默塞德分校的陈阳泉教授。

谢谢你看我的文章，我们，下次再见。

> / 作者，卢传范
> / 欢迎交流，请联系邮箱，lucf2023@mail.sustech.edu.cn
